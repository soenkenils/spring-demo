spring.application.name=spring-demo-test

# Die tatsächlichen Verbindungsdaten werden automatisch von Spring Boot Testcontainers gesetzt
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway Konfiguration für Tests
spring.flyway.enabled=true
